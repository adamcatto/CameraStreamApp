<UserControl x:Class="CameraStream.Windows.Views.WorkspaceEditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignHeight="450" d:DesignWidth="800">
    <ScrollViewer>
        <StackPanel Margin="10">
            <TextBlock Text="Workspace name" />
            <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" />

            <TextBlock Text="Jump host (optional, e.g. user@jump.example)" Margin="0,10,0,0" />
            <TextBox Text="{Binding JumpHost, UpdateSourceTrigger=PropertyChanged}" />

            <TextBlock Text="Cameras" FontWeight="Bold" Margin="0,15,0,5" />

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="90" />
                    <ColumnDefinition Width="30" />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="Camera name" FontWeight="Bold" />
                <TextBlock Grid.Column="1" Text="IP address" FontWeight="Bold" Margin="5,0,0,0" />
                <TextBlock Grid.Column="2" Text="Username" FontWeight="Bold" Margin="5,0,0,0" />
            </Grid>

            <ItemsControl ItemsSource="{Binding Cameras}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="0,2">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="90" />
                                <ColumnDefinition Width="30" />
                            </Grid.ColumnDefinitions>
                            <TextBox Grid.Column="0" Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" />
                            <TextBox Grid.Column="1" Text="{Binding Host, UpdateSourceTrigger=PropertyChanged}" Margin="5,0,0,0" />
                            <TextBox Grid.Column="2" Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" Margin="5,0,0,0" />
                            <Button Grid.Column="3" Content="-" Margin="5,0,0,0"
                                    Command="{Binding DataContext.RemoveCameraCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                    CommandParameter="{Binding}" />
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <Button Content="Add camera" Command="{Binding AddCameraCommand}" Margin="0,10,0,0" HorizontalAlignment="Left" />
        </StackPanel>
    </ScrollViewer>
</UserControl>
