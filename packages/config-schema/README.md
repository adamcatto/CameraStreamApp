# Shared configuration contract

`workspaces.schema.json` documents the workspace JSON exchanged by the macOS,
Windows, and web apps. Platform code owns its runtime model, while this package
is the canonical serialized shape for imports, exports, fixtures, and future
code generation.
